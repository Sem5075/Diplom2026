server:
  replicas: 1
  service:
    type: ClusterIP
  extraArgs:
    - --insecure          

configs:
  params:
    server.insecure: true
    
redis-ha:
  enabled: false

controller:
  replicas: 1

repoServer:
  replicas: 1

applicationSet:
  replicas: 1